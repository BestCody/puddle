import { requireUser } from '@/lib/auth/user'
import { renderProductPage } from '@/lib/app/render-product-page'
import { AppealForm } from '@/components/appeal-form'
export const dynamic='force-dynamic';export const metadata={title:'Appeals',robots:{index:false,follow:false}}
export default async function AppealsPage(){const session=await requireUser({onboarding:true});const{data}=await session.supabase.from('moderation_appeals').select('id,case_id,state,statement,decision_reason,created_at,decided_at,moderation_cases(case_number,title)').eq('appellant_id',session.user.id).order('created_at',{ascending:false});return renderProductPage(async()=> <><div className="page-heading-row"><div><span className="section-pill section-pill-yellow">Appeals</span><h1 className="product-title">Request another review.</h1><p>Appeals are assigned separately and preserve the original case evidence and audit history.</p></div></div><AppealForm/><section className="admin-card"><h2>Your appeals</h2>{(data||[]).map((item)=><article key={item.id}><strong>{item.moderation_cases?.case_number} · {item.state}</strong><p>{item.statement}</p>{item.decision_reason?<small>{item.decision_reason}</small>:null}</article>)}</section></>)}
