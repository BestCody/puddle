import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { isSupabaseConfigured } from '@/lib/supabase/env'
import { pathWithMessage } from './redirect'
export async function getCurrentUser(){if(!isSupabaseConfigured())return{user:null,profile:null,configured:false};const supabase=await createClient();const{data:{user},error}=await supabase.auth.getUser();if(error||!user)return{user:null,profile:null,configured:true};const{data:profile}=await supabase.from('profiles').select('id,display_name,username,birth_date,city,search_radius_km,bio,avatar_path,latitude,longitude,profile_visibility,activity_visibility,allow_friend_requests,allow_event_messages,onboarding_completed_at,suspended_at,role,interests').eq('id',user.id).maybeSingle();return{user,profile,configured:true,supabase}}
export async function requireUser({onboarding=false}={}){const r=await getCurrentUser();if(!r.configured)redirect(pathWithMessage('/signin','error','Accounts are temporarily unavailable. Please try again later.'));if(!r.user)redirect('/signin?next=/discover');if(r.profile?.suspended_at){await r.supabase.auth.signOut();redirect(pathWithMessage('/signin','error','This account is suspended. Contact support for help.'))}if(onboarding&&!r.profile?.onboarding_completed_at)redirect('/onboarding');return r}
