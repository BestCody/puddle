name: Read-only B2 version history diagnostic

on:
  push:
    branches: [b2-candidate-consistency-recovery-v2]

permissions:
  contents: read
  statuses: write

# Temporary branch-only diagnostic. It performs no B2 writes.
jobs:
  inspect-failed-b2-object:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      B2_DATA_APPLICATION_KEY_ID: ${{ secrets.B2_DATA_APPLICATION_KEY_ID || secrets.B2_KEY_ID }}
      B2_DATA_APPLICATION_KEY: ${{ secrets.B2_DATA_APPLICATION_KEY || secrets.B2_APPLICATION_KEY }}
      B2_DATA_BUCKET_NAME: ${{ vars.B2_DATA_BUCKET_NAME || secrets.B2_BUCKET || 'puddle-assets' }}
      B2_DATA_S3_ENDPOINT: ${{ secrets.B2_DATA_S3_ENDPOINT || secrets.B2_S3_ENDPOINT }}
      B2_DATA_S3_REGION: ${{ vars.B2_DATA_S3_REGION || secrets.B2_REGION || 'us-east-005' }}
      B2_DATA_PREFIX: data
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Install diagnostic dependencies
        run: python -m pip install --disable-pip-version-check boto3 brotli duckdb 'h3>=4,<5' orjson zstandard
      - id: diagnose
        name: Check exact historical version for failed geo shard
        continue-on-error: true
        shell: bash
        run: |
          set +e
          python scripts/global-data/repair_b2_search_candidate.py \
            --snapshot=2026-08-17 \
            --key=data/search/schema=v1/snapshot=2026-08-17/geo/r6/8611aa717ffffff.json.br \
            2>&1 | tee /tmp/b2-history-diagnostic.log
          status=${PIPESTATUS[0]}
          if grep -q 'repair_history_exact_match=true' /tmp/b2-history-diagnostic.log; then
            echo 'outcome=found' >> "$GITHUB_OUTPUT"
          elif grep -q 'repair_history_exact_match=false' /tmp/b2-history-diagnostic.log; then
            echo 'outcome=missing' >> "$GITHUB_OUTPUT"
          else
            echo 'outcome=error' >> "$GITHUB_OUTPUT"
          fi
          exit "$status"
      - name: Publish diagnostic commit status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          OUTCOME: ${{ steps.diagnose.outputs.outcome }}
          STATUS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os, urllib.request
          outcome = os.environ.get('OUTCOME') or 'error'
          if outcome == 'found':
              state = 'success'
              description = 'Exact ledger-matching B2 history version exists'
          elif outcome == 'missing':
              state = 'failure'
              description = 'No exact ledger-matching B2 history version exists'
          else:
              state = 'error'
              description = 'B2 history diagnostic could not complete'
          url = f"https://api.github.com/repos/{os.environ['GITHUB_REPOSITORY']}/statuses/{os.environ['GITHUB_SHA']}"
          body = json.dumps({
              'state': state,
              'context': 'B2 history diagnostic',
              'description': description,
              'target_url': os.environ['STATUS_URL'],
          }).encode()
          request = urllib.request.Request(url, data=body, method='POST', headers={
              'Authorization': f"Bearer {os.environ['GH_TOKEN']}",
              'Accept': 'application/vnd.github+json',
              'X-GitHub-Api-Version': '2022-11-28',
              'Content-Type': 'application/json',
          })
          with urllib.request.urlopen(request, timeout=30) as response:
              print(response.status)
          PY
      - name: Fail if exact history is unavailable
        if: always() && steps.diagnose.outputs.outcome != 'found'
        run: exit 1
