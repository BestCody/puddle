name: Apply account deletion integrity

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: apply-account-deletion-integrity
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v3.0.0
        with:
          version: latest

      - name: Apply account deletion integrity migration
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SUPABASE_ACCESS_TOKEN"
          supabase db query \
            --linked \
            --project-ref cegoqtvajwajczbofpep \
            --file supabase/migrations/20260829130000_account_deletion_integrity.sql
