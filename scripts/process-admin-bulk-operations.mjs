import { createClient } from '@supabase/supabase-js'
import { processBulkOperations } from '../lib/security/bulk-worker.js'
const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SECRET_KEY;if(!url||!key)throw new Error('Supabase secret-key credentials are required.');const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const results=await processBulkOperations(admin,Number(process.env.MODERATION_BULK_BATCH_SIZE||25));console.log(JSON.stringify({processed:results.length,failed:results.filter((x)=>!x.ok).length},null,2));if(results.some((x)=>!x.ok))process.exitCode=1
