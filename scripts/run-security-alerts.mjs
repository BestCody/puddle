import { createClient } from '@supabase/supabase-js'
const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('Supabase service-role credentials are required.');const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const [sla,anomaly]=await Promise.all([admin.rpc('run_moderation_sla_alerts_v1'),admin.rpc('detect_security_anomalies_v1')]);if(sla.error)throw sla.error;if(anomaly.error)throw anomaly.error;console.log(JSON.stringify({slaAlerts:sla.data,anomalyAlerts:anomaly.data},null,2))
