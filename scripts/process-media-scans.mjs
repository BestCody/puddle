import { createClient } from '@supabase/supabase-js'
import { processPendingMediaScans } from '../lib/security/media-scan-worker.js'
const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('Supabase service-role credentials are required.');const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const results=await processPendingMediaScans(admin,Number(process.env.MEDIA_SCAN_BATCH_SIZE||25));console.log(JSON.stringify({processed:results.length,failed:results.filter((x)=>!x.ok).length},null,2));if(results.some((x)=>!x.ok))process.exitCode=1
