import { access,readFile } from 'node:fs/promises'
import { execFileSync } from 'node:child_process'
import { join } from 'node:path'
import { fileURLToPath } from 'node:url'
const root=fileURLToPath(new URL('..',import.meta.url)),required=['app/stage-six.css','app/stage-seven.css','app/social/actions.js','app/api/location-sharing/route.js','app/api/location-sharing/expire/route.js','components/listing-social.js','components/realtime-conversation.js','components/temporary-location-sharing.js','lib/app/social-data.js','scripts/process-notification-outbox.mjs','scripts/expire-location-sharing.mjs','supabase/migrations/0011_social_coordination.sql','supabase/migrations/0012_temporary_location_sharing.sql','supabase/migrations/0013_stage7_worker_access.sql','supabase/tests/0011_stage6_authorization.sql','supabase/tests/0012_stage7_authorization.sql','docs/STAGE_7_SETUP.md'];for(const p of required)await access(join(root,p));for(const p of['app/social/actions.js','app/api/location-sharing/route.js','app/api/location-sharing/expire/route.js','lib/app/social-data.js','scripts/process-notification-outbox.mjs','scripts/expire-location-sharing.mjs'])execFileSync(process.execPath,['--check',join(root,p)],{stdio:'pipe'});const s6=await readFile(join(root,'supabase/migrations/0011_social_coordination.sql'),'utf8'),s7=await readFile(join(root,'supabase/migrations/0012_temporary_location_sharing.sql'),'utf8'),worker=await readFile(join(root,'supabase/migrations/0013_stage7_worker_access.sql'),'utf8');for(const m of['search_social_profiles_v1','notification_outbox','open_direct_conversation_v1','send_message_v1'])if(!s6.includes(m))throw new Error(`Stage 6 missing ${m}`);for(const m of['location_viewer_snapshots','start_location_share_v1','stop_location_share_v1','expire_location_shares_v1','blocks_revoke_location_sharing'])if(!s7.includes(m))throw new Error(`Stage 7 missing ${m}`);if(!worker.includes('to service_role'))throw new Error('Stage 7 worker grant missing');const panel=await readFile(join(root,'components/temporary-location-sharing.js'),'utf8');for(const m of['watchPosition','confirmed','approximate','venue','precise','Stop now','I’m here','Directions between stops'])if(!panel.includes(m))throw new Error(`Location UI missing ${m}`);console.log('Puddle Stage 6 repair and Stage 7 validation checks passed.')
